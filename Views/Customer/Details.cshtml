@model MovieApp.Models.Customer
@{
    ViewBag.Title = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@Model.Name</h2>
<h2>@Model.membershiptype.DurationMonth</h2
if (@Model.DoB.HasValue)
{ 
<h2>Date of Birth : @Model.DoB.ToString()</h2>
}